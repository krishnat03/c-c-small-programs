#include <bits/stdc++.h>
using namespace std;
int main()
{
    system("cls");
    int budget = 105;
    vector<int> price = {10, 10, 40, 50, 90};
    vector<int> quantity = {10, 20, 20, 50, 150};
    int tempBudget = budget;
    int tempQuantity = 0;
    for (int i = 0; i < price.size(); i++)
    {
        tempBudget = budget;
        for (int j = 0; j < price.size(); j++)
        {
            
        }
        
    }

    return 0;
}